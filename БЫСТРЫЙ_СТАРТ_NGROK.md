# 🚀 Быстрый старт с ngrok

## Шаг 1: Регистрация (1 минута)

1. Откройте https://ngrok.com/
2. Нажмите "Sign up" (бесплатно)
3. Зарегистрируйтесь

## Шаг 2: Получите токен

1. После входа откройте: https://dashboard.ngrok.com/get-started/your-authtoken
2. Скопируйте ваш authtoken

## Шаг 3: Настройте ngrok

Выполните команду (замените YOUR_TOKEN на ваш токен):

```bash
ngrok config add-authtoken YOUR_TOKEN
```

## Шаг 4: Запустите туннель

### Вариант А: Автоматический (рекомендуется)

```bash
node start-ngrok.js
```

Скрипт автоматически:
- Запустит ngrok
- Получит HTTPS URL
- Обновит .env файл
- Покажет инструкции

### Вариант Б: Вручную

```bash
ngrok http 3000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

Обновите `.env`:
```env
NEXTAUTH_URL="https://abc123.ngrok.io"
```

## Шаг 5: Перезапустите сервер

```bash
npm run dev
```

## Шаг 6: Тестирование на iPhone

### На iPhone:

1. Откройте **Safari**
2. Перейдите на ваш **ngrok URL** (показан в консоли)
3. **Войдите:**
   - Email: `test@test.com`
   - Пароль: `test1234`
4. **Установите PWA:**
   - Кнопка "Поделиться" → "На экран «Домой»"
5. **Запустите через иконку** на домашнем экране
6. **Разрешите уведомления**

### Проверка:

```bash
node test-push-notification.js
```

Должно прийти уведомление на iPhone! 🎉

## ⚠️ Важно

- Бесплатный ngrok закрывается через 2 часа
- При перезапуске URL изменится
- Нужно будет обновить .env и переустановить PWA

## 🐛 Проблемы?

### "ERR_NGROK_108":
```bash
ngrok config add-authtoken YOUR_TOKEN
```

### Уведомления не приходят:
1. Проверьте iOS 16.4+
2. Приложение установлено как PWA?
3. Настройки → Уведомления → [Приложение] → ВКЛ

### Приложение не открывается:
- Используйте HTTPS URL (не HTTP)
- Проверьте, что ngrok и dev-сервер запущены

## 📚 Подробная документация

- `NGROK_SETUP.md` - полная инструкция
- `IOS_PUSH_DIAGNOSTICS.md` - диагностика проблем
- `РЕШЕНИЕ_IOS_PUSH.md` - общая информация
